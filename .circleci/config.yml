version: 2.1
commands:
  docker-login:
    steps:
      - run: echo $DOCKER_HUB_TOKEN | docker login --username $DOCKER_HUB_USER --password-stdin
jobs:
  local-install-test:
    machine: true
    steps:
      - docker-login
      - checkout
      - run: docker-compose run development /home/docker/src/github.com/cohei/dotfiles/install.sh
  network-install-test:
    machine: true
    steps:
      - docker-login
      - checkout
      - run: docker-compose up test
workflows:
  version: 2
  testing:
    jobs:
      - local-install-test:
          context:
            - docker-hub-creds
      - network-install-test:
          filters:
            branches:
              only:
                - master
          context:
            - docker-hub-creds
